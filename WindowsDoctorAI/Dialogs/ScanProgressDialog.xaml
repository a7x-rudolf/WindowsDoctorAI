<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="WindowsDoctorAI.Dialogs.ScanProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsDoctorAI.Dialogs"
    Title=""
    CloseButtonText=""
    PrimaryButtonText=""
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">640</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">560</x:Double>
    </ContentDialog.Resources>

    <Grid Width="580" MinHeight="500" MaxHeight="640">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- HEADER: Hero icon + title + progress bar -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,20">
            <!-- Hero icon -->
            <Border Width="64" Height="64" CornerRadius="16"
                    Background="{StaticResource AccentGradientBrush}"
                    HorizontalAlignment="Center">
                <FontIcon x:Name="HeroIcon" Glyph="&#xE773;" FontSize="28" Foreground="White" />
            </Border>

            <TextBlock x:Name="TitleText"
                       Text="Diagnostic Scan Running"
                       FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" />

            <TextBlock x:Name="SubtitleText"
                       Text="Analyzing your system components..."
                       FontSize="13"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <!-- PROGRESS BAR -->
        <StackPanel Grid.Row="1" Spacing="8" Margin="0,0,0,16">
            <ProgressBar x:Name="ProgressBar"
                         Value="0" Maximum="100" MinHeight="8"
                         CornerRadius="4" />
            <Grid>
                <TextBlock x:Name="ProgressLabelText" Text="Initializing..."
                           FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Left" />
                <TextBlock x:Name="ProgressPercentText" Text="0%"
                           FontSize="12" FontWeight="SemiBold"
                           Foreground="{StaticResource BrandAccentBrush}"
                           HorizontalAlignment="Right" />
            </Grid>
        </StackPanel>

        <!-- CATEGORY LIST + LOG -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="16">
                <!-- Category list -->
                <StackPanel x:Name="CategoryList" Spacing="4">
                    <!-- populated in code-behind -->
                </StackPanel>

                <!-- Live log -->
                <StackPanel Spacing="6">
                    <Grid>
                        <TextBlock Text="ACTIVITY LOG"
                                   FontSize="11" FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   CharacterSpacing="80"
                                   HorizontalAlignment="Left" />
                        <StackPanel Orientation="Horizontal" Spacing="4"
                                    HorizontalAlignment="Right">
                            <Ellipse Width="6" Height="6" Fill="{StaticResource StatusCriticalBrush}"
                                     VerticalAlignment="Center" />
                            <TextBlock Text="LIVE" FontSize="10" FontWeight="Bold"
                                       Foreground="{StaticResource StatusCriticalBrush}" />
                        </StackPanel>
                    </Grid>

                    <Border Background="{ThemeResource ControlFillColorTertiaryBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="12"
                            MinHeight="140"
                            MaxHeight="200">
                        <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="LogPanel" Spacing="4" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>